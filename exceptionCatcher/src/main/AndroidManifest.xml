<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!--
        usesCleartextTraffic is not recommended and is used here
        to allow the app sending traffic without knowing the server
        IP in advance (in the case of a physical device) if we know the IP
        in advance we would use <android:networkSecurityConfig="@xml/network_security_config">
    -->
    <application
        android:usesCleartextTraffic="true">
    </application>

</manifest>